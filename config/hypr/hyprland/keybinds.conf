# KEYBINDS

# KEYBIND FLAGS (MODIFIERS) Example: bind[flags] = ...
# l	    locked	            Will also work when an input inhibitor (e.g. a lockscreen) is active.
# r	    release	            Will trigger on release of a key.
# c	    click	            Will trigger on release of a key or button as long as the mouse cursor stays inside binds:drag_threshold.
# g	    drag	            Will trigger on release of a key or button as long as the mouse cursor moves outside binds:drag_threshold.
# o	    long press	        Will trigger on long press of a key.
# e	    repeat	            Will repeat when held.
# n	    non-consuming	    Key/mouse events will be passed to the active window in addition to triggering the dispatcher.
# m	    mouse	            See the dedicated Mouse Binds section.
# t	    transparent	        Cannot be shadowed by other binds.
# i	    ignore mods	        Will ignore modifiers.
# s	    separate	        Will arbitrarily combine keys between each mod/key, see Keysym combos.
# d	    has description	    Will allow you to write a description for your bind.
# p	    bypass	            Bypasses the app’s requests to inhibit keybinds.
$mainMod = SUPER
$ALT = ALT
$CTRL = CTRL
$SUPER = SUPER

submap = reset

    # Desktop zooming or magnifier
    # bind = $mainMod ALT, mouse_down, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor * 1.2}')"
    # bind = $mainMod ALT, mouse_up, exec, hyprctl keyword cursor:zoom_factor "$(hyprctl getoption cursor:zoom_factor | awk 'NR==1 {factor = $2; if (factor < 1) {factor = 1}; print factor / 1.2}')" 

    # Rofi
    bind = $mainMod, TAB, exec, rofi -show drun													 #==> Apps Manager
    bind = $mainMod, Z, exec, sh ~/.config/hypr/hyprland/scripts/rofimoji.sh 				     #==> Rofi Emoji

    # Reload hyprland
    bind = $mainMod ALT, R, exec, hyprctl reload && canberra-gtk-play -i audio-volume-change -d "volume-change"

    # Forçar o monitor a ligar/desligar sinal (ajuda no handshake da TV)
    bind = $mainMod, F12, exec, hyprctl dispatch dpms toggle
    bind = $mainMod, F11, exec, hyprctl dispatch forcerendererreload

    # Audio control
    binde = , F11, exec, ~/.config/hypr/hyprland/scripts/volume-control.sh down
    binde = , F12, exec, ~/.config/hypr/hyprland/scripts/volume-control.sh up

    # OCR
    bindd = SUPER+SHIFT, A, Character recognition, exec, ~/.config/hypr/hyprland/scripts/ocr.sh

    # Screenshot
    # bindld = ,PRINT, Screenshot >> clipboard ,exec, grim - | wl-copy # Screenshot >> clipboard
    bindld = , PRINT, Screenshot >> clipboard & save, exec, mkdir -p $(xdg-user-dir PICTURES)/Screenshots && grim $(xdg-user-dir PICTURES)/Screenshots/Screenshot_"$(date '+%Y-%m-%d_%H.%M.%S')".png # Screenshot >> clipboard & file
    bindd = $SUPER+SHIFT, S, Screen snip, exec, pidof slurp || hyprshot --freeze --clipboard-only --mode region --silent

    #programs
    bind = $ALT, F1, exec, flatpak run io.missioncenter.MissionCenter
    bind = $ALT, F2, exec, kitty s-tui
    bind = $ALT, F3, exec, lact
    bind = $mainMod $ALT, X, exec, hyprctl kill # Forcefully zap a window
    bind = $ALT, ESCAPE, killactive
    bind = $ALT, Q, exec, kitty --title main
    bind = $mainMod, APOSTROPHE, exec, ~/.config/hypr/hyprland/scripts/switch-audio-sink.sh && canberra-gtk-play -i audio-volume-change -d "volume-change"
    bind = $ALT, W, exec, pavucontrol
    bind = $ALT, E, exec, thunar
    bind = $ALT, R, exec, firefox
    bind = $ALT, C, exec, code
    bind = $mainMod, C, exec, gnome-calculator
    bind = $ALT, 1, exec, ~/.config/hypr/hyprland/scripts/launch_suite.sh
    bind = $ALT, 2, exec, steam
    bind = $ALT, 3, exec, flatpak run com.spotify.Client
    bind = $ALT, 4, exec, flatpak run com.stremio.Stremio
    # Updates
    # bind = $ALT, F9, exec, kitty sudo dnf update discord

    # Shutdown menu
    bind = $CTRL+SHIFT, ESCAPE, exec, kitty systemctl poweroff -i
    bind = $CTRL+SHIFT, APOSTROPHE, exec, kitty reboot

    # Fullscreen
    bind = $mainMod, F, fullscreen

    # Floating window
    bind = $mainMod, SPACE, togglefloating
    bind = $mainMod ALT, SPACE, exec, hyprctl dispatch workspaceopt allfloat

    # Move windows between workspaces
    bind = $ALT, A, movetoworkspacesilent, 1
    bind = $ALT, S, movetoworkspacesilent, 2
    bind = $ALT, D, movetoworkspacesilent, 3
    bind = $ALT, F, movetoworkspacesilent, 4
    bind = $ALT, G, movetoworkspacesilent, 6

    # Scroll through existing workspaces
    # bind = $mainMod, mouse_down, workspace, e+1
    # bind = $mainMod, mouse_up, workspace, e-1

    # Navegação inteligente de workspaces (pulando o 5)
    bind = $ALT, TAB, exec, bash ~/.config/hypr/hyprland/scripts/cycle_workspaces.sh next
    bind = $ALT SHIFT, TAB, exec, bash ~/.config/hypr/hyprland/scripts/cycle_workspaces.sh prev
    bind = $ALT, APOSTROPHE, workspace, emptynm

    # Move/resize windows with mainMod + LMB/RMB and dragging
    bindm = $mainMod, mouse:272, movewindow
    bindm = $mainMod, mouse:273, resizewindow

    bind = , XF86AudioPlay, exec, playerctl play-pause
    bind = , XF86AudioNext, exec, playerctl next
    bind = , XF86AudioPrev, exec, playerctl previous

    # Switch workspaces with mainMod + [0-9]
    # bind = $mainMod, 1, workspace, 1

    # Move active window to a workspace with mainMod + SHIFT + [0-9]
    # bind = $mainMod SHIFT, 1, movetoworkspace, 1

    # Move window silently to workspace Super + Alt + [0-9]
    # bind = $mainMod ALT, 1, movetoworkspacesilent, 1

    # Toggle dos Workspaces Especiais
    bind = , F8, togglespecialworkspace, stash
    bind = , F6, workspace, 5
    bind = $ALT, F6, movetoworkspacesilent, 5

    bind = , Pause, exec, ~/.config/hypr/hyprland/scripts/toggle_scanout_pinger.sh

# Definição do Submap (O script vai ativar isso aqui sozinho)
submap = gaming
    bind = , F6, workspace, previous
    bind = , F8, togglespecialworkspace, stash
    bind = $ALT, ESCAPE, killactive
    bind = $mainMod $ALT, X, exec, hyprctl kill # Forcefully zap a window
    bind = $mainMod, F, fullscreen
    bindm = $mainMod, mouse:272, movewindow
    bindm = $mainMod, mouse:273, resizewindow
    bind = , XF86AudioPlay, exec, playerctl play-pause
    bind = , XF86AudioNext, exec, playerctl next
    bind = , XF86AudioPrev, exec, playerctl previous

    binde = , F11, exec, ~/.config/hypr/hyprland/scripts/volume-control.sh down
    binde = , F12, exec, ~/.config/hypr/hyprland/scripts/volume-control.sh up
    bind = $mainMod, APOSTROPHE, exec, ~/.config/hypr/hyprland/scripts/switch-audio-sink.sh && canberra-gtk-play -i audio-volume-change -d "volume-change"

    bind = , Pause, exec, ~/.config/hypr/hyprland/scripts/toggle_scanout_pinger.sh

    # Reload hyprland
    bind = $mainMod ALT, R, exec, hyprctl reload && canberra-gtk-play -i audio-volume-change -d "volume-change"

    # Forçar o monitor a ligar/desligar sinal (ajuda no handshake da TV)
    bind = $mainMod, F12, exec, hyprctl dispatch dpms toggle
    bind = $mainMod, F11, exec, hyprctl dispatch forcerendererreload
    bindld = , PRINT, Screenshot >> clipboard & save, exec, mkdir -p $(xdg-user-dir PICTURES)/Screenshots && grim $(xdg-user-dir PICTURES)/Screenshots/Screenshot_"$(date '+%Y-%m-%d_%H.%M.%S')".png # Screenshot >> clipboard & file
    bindd = $SUPER+SHIFT, S, Screen snip, exec, pidof slurp || hyprshot --freeze --clipboard-only --mode region --silent